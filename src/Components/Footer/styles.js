import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  footer: {
    color: "#8e95a5",
  },

  border: {
    borderTop: "0.1rem solid #8e95a5",
    marginBottom: "1rem",
  },

  trademark: {
    marginTop: "1rem",
  },
}));

export default useStyles;
